


function Rover(input){
    this.input = input;
    this.IntPostion = [0, 0];
    this.IntDirection = 'N';
    this.CurrentDirection = '';
    this.CurrentPostion = [];
    this.CurrentLocation = '';
    this.GetCurrentPostionAndDirection = function(input){

        const inputArray = input.str("");
        const IntX = IntPostion.splice(1,1);
        const IntY = IntPostion.splice(0,0);

        inputArray.forEach(function(command){
             if(command = L & CurrentDirection = 'N'){
                CurrentDirection =
        });
        if (command = M) {

            Rover.CurrenntPostion[0] = Rover.IntPostion + 1
    };
    return 
}
}

const rover1 = new Rover('LMR');

console.log(rover1);